# web-level-meter

# 概要

* Webベースのオーディオレベルメータ、ライブ配信Webアプリ向けのレベルチェックを気軽に行える。
* ステレオ状況、不正な結線などをチェック。

# 機能

### オーディオデバイス選択

選択中オーディオデバイスを押すとオーディオデバイスを選択できるダイアログが表示され、切り替えられる。

### 対数表示のステレオピークレベルメータ

ピークレベルメータはステレオで、対数表示とする。

### ラウドネスメータ

ITU BS.1770準拠のラウドネスメータで、ターゲットは-15LUFS固定。メータとして表示するのはMomentaryのみ

### わかりやすい適正表示

 初心者にもわかりやすいように、Short Termが-16LUFS以下で「小さい」、-14LUFS以上で「大きい」、その間で「ちょうどいい」と表示される。

### -1.0dBTPを超えたときのピークランプ(リセットボタン付き)

-1.0dbTPを超えた場合、ピークLEDが点灯し、リセットされるまで点灯。

### ステレオ状態の検出

左右が同じ信号の場合は「モノラル」、片チャンネルだけ信号がある場合は「片チャンネル」、左右が逆位相の場合は「逆位相」、それ以外は「ステレオ」と表示される。

# 実装上のポイント

* ブラウザのノイズキャンセリング・オートゲインコントロール・エコーリダクションは必ずOFFにする。

* ダークグレーを基調とした、業務用オーディオ機器、DTM機材を彷彿とさせるプロフェッショナルなクールなデザイン

* 技術スタックは Vite + React で、公開はスタティックなファイルのみでリリースする。

* Github Pagesにて公開する。

* ローカルでのビルドなどの指示はMakefileに集約。

* README.md、コミットコメントなどはすべて日本語で。
